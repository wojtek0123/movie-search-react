export type FetchData = {
	description: string;
	id: string;
	image: string;
	title: string;
};

export type Fetch = {
	description: string;
	id: string;
	image: string;
	resultType: string;
	title: string;
};